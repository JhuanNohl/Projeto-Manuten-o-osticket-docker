<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<title>Termo de Garantia e Política de Manutenção — ZKTeco do Brasil</title>
<style>
  :root{ --zk-green:#7AC143; --zk-graphite:#2b3630; --zk-text:#2c2c2c; --zk-line:#e3e7e1; }
  body{
    margin:0; padding:0; background:#f7f9f6; color:var(--zk-text);
    font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Helvetica,Arial,sans-serif;
    line-height:1.6;
  }
  .zk-doc{ max-width:760px; margin:0 auto; padding:40px 28px 64px; }
  .zk-doc-header{
    border-bottom:3px solid var(--zk-green); padding-bottom:18px; margin-bottom:28px;
  }
  .zk-doc-header h1{
    margin:0; font-size:22px; line-height:1.35; color:var(--zk-graphite);
  }
  .zk-doc-intro{ font-size:14.5px; color:#4a4a4a; }
  h2{
    font-size:17px; color:var(--zk-graphite); border-left:4px solid var(--zk-green);
    padding-left:10px; margin:34px 0 12px;
  }
  h3{ font-size:14.5px; color:var(--zk-graphite); margin:22px 0 8px; }
  p{ font-size:14px; margin:0 0 12px; }
  ul{ font-size:14px; margin:0 0 14px; padding-left:22px; }
  li{ margin-bottom:4px; }
  hr{ border:0; border-top:1px solid var(--zk-line); margin:26px 0; }
  strong{ color:var(--zk-graphite); }
  blockquote{
    background:#fff; border:1px solid var(--zk-line); border-left:4px solid var(--zk-green);
    border-radius:6px; padding:14px 18px; margin:14px 0; font-size:13.5px;
  }
  blockquote p{ margin:0 0 4px; }
  .zk-doc-footer{
    margin-top:36px; padding-top:16px; border-top:1px solid var(--zk-line);
    font-size:12px; color:#777; font-style:italic;
  }
  @media print{ body{ background:#fff; } }
</style>
</head>
<body>
<div class="zk-doc">
<?php require __DIR__ . '/../../include/client/zk-termo-manutencao-content.inc.php'; ?>
</div>
</body>
</html>
